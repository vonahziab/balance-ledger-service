# Исходное задание

Текст ТЗ без изменений.

> Необходимо сделать небольшой web server
>
> Обязательно:
> postgres
>
> Не обязательно, но будет плюсом:
> любые дополнения вроде кеширования, валидации и др.
>
> Endpoint :
> есть табличка users с полями id и balance, там есть один юзер с id = 1
> вторая табличка с историей изменения платежа (user_id, action, amount, ts)
>
> Нужно реализовать списание баланса пользователя
>
> Пример: юзер покупает предмет на сайте за $100
> пересчет баланса по истории после каждой операции в поле balance
>
> Базу можно не поднимать, просто показать код как это может работать, решение нужно залить на git
>
> Популярные вопросы - делать на typescript, nest - можно, даже нужно

## Где закрыт каждый пункт

| Требование | Где |
|------------|-----|
| Postgres | TypeORM + миграции — [решения](architecture.md#решения) |
| `users (id, balance)`, пользователь `id = 1` | сидирующая миграция — [решения](architecture.md#решения) |
| История `(user_id, action, amount, ts)` | `balance_ledger` — [модель данных](architecture.md#модель-данных) |
| Списание баланса | `POST /users/:id/debit` — [поток запроса](architecture.md#поток-запроса-post-usersiddebit) |
| Пересчёт баланса по истории после каждой операции в поле `balance` | [ADR-0004](adr/0004-balance-recalculation-from-ledger.md) |
| кэширование (плюс) | Redis для `GET /users/:id/balance` — [поток запроса](architecture.md#поток-запроса-get-usersidbalance) |
| Валидация (плюс) | `:id`, `amount`, `Idempotency-Key` — [обработка ошибок](architecture.md#обработка-ошибок) |
| Другие дополнения | идемпотентность ([ADR-0002](adr/0002-idempotency.md)), конкурентность ([ADR-0003](adr/0003-concurrency.md)), тесты ([решения](architecture.md#решения)), [безопасность](architecture.md#безопасность), [логирование](architecture.md#логирование) |
| TypeScript + Nest | [стек](architecture.md#стек) |
